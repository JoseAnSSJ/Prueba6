<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pablo.prueba6">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Orden"></activity>
        <activity android:name=".UserConfig"></activity>
        <activity
            android:name=".acivity_navegation"
            android:label="@string/title_activity_acivity_navegation"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".Reportes"/>
        <activity android:name=".User.UserController">
        </activity>
        <activity android:name=".Request.RequestTecnico"/>

    </application>

</manifest>